syntax = "proto3";

package math;

option go_package = "proto;math";  // go_package 옵션 추가

service MathService {
  rpc Add (AddRequest) returns (AddResponse);
}

message AddRequest {
  int32 a = 1;
  int32 b = 2;
}

message AddResponse {
  int32 sum = 1;
}